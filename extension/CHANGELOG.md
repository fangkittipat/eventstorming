# Changelog

## 0.1.10

- Clicking a `.storm` file opens the paper-note board again (reads the file itself, so Cursor no longer hits Assertion Failed)
- **Open Source** still shows the Storm text if you want the diff review

## 0.1.9

- Add `aggregate` sticky (light yellow); keep actor yellow
- Keep the board editor open if a sticky kind is missing from an older build

## 0.1.8

- Fix Marketplace screenshot URLs so example images load in Cursor

## 0.1.7

- Marketplace listing with board screenshots, language table, and examples

## 0.1.6

- First Marketplace-ready release
- Open `.storm` files in a split source + paper-note board editor
- Render `eventstorm` / `storm` / `eventstorming` fences in Markdown preview
- Export boards as SVG or PNG
